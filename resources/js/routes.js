import Home from '@/Components/Home.vue';
import Login from '@/Components/Login.vue';

const routes = [
    {
        path: '/',
        component: Home
    },
    {
        path: '/login',
        component: Login
    }
];

export default routes;
